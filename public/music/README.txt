Drop the wedding background song here as `bg.mp3`.
The player (`src/components/wedding/BgMusic.tsx`) loads it from `music/bg.mp3`,
starts it when the guest taps the seal, loops it, and offers a mute toggle.
